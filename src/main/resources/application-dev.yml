spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:./local_database/database
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create

  h2:
    console:
      enabled: true
      path: /h2-console